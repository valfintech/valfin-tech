# Roofing Lead Automation — Deliverables

n8n + Claude API + Google Sheets CRM for a single Boston-area roofing company. See the project brief (`CLAUDE.md`) for the full spec.

> **New Claude session? Start here, before anything else.** `docs/VALFIN_MASTER_CONTEXT_PROMPT.md` (added 2026-06-11) is the canonical onboarding document — what Valfin is, its mission and positioning, the V1 → V1.1 evolution, every workflow/doc/template and where it lives, the full founder-led sales→deployment→support→renewal lifecycle, known non-blocking open items, and explicit instructions for how future sessions should operate (don't proactively invent features — see its §15). Read it first, in full, before consulting anything else in this repo or relying on memory from a prior conversation.
>
> **Signing a new client? Open `docs/CLIENT_CLONING_MASTER_PROMPT.md`** (added 2026-06-11) — the self-sufficient operational prompt for deploying a client's instance of the Valfin system: what's reusable framework vs. industry-specific customization, every workflow's mandatory/optional/CONFIG/email-SMS status, the full client intake variable list, every account/credential/approval needed, the exact step-by-step cloning sequence, testing/validation procedures, and go-live / post-launch / renewal / acceptance checklists — including guidance for cloning into entirely different industries (HVAC, plumbing, dental, legal, etc.).
>
> Together, these two documents are designed so that **no future Claude session depends on this repo's conversation history** — the repository itself is the institutional memory. See `docs/ROADMAP.md`'s 2026-06-11 entry for why this transition was made.

## Folder layout
```
workflows/   importable n8n workflow JSON exports — all 13 workflows now have one (closed 2026-06-08,
             workflow 13 added 2026-06-12; see ROADMAP), plus one retained `.ts` SDK-source file for
             workflow 11 (kept for its design-rationale comments — the `.json` export is the actual
             importable artifact)
prompts/     Claude system prompts (reference copies; live copies live in the workflow Code nodes)
docs/        setup + operating guides
```

## Build status
| Phase | Status | n8n |
|---|---|---|
| 1. Google Sheets CRM | done (pre-existing) | — |
| 2. Missed-call + form capture | ✅ **Complete — live in n8n** | 3 workflows deployed |
| 3. Lead response + follow-up automation | ✅ **Complete — verified live** | 5/5 components live |
| 4. Reminders / reschedule / cancel | ✅ **Complete — 2/2 live & tested**, plus Workflow 13 (Appointment Reschedule Notifier, added 2026-06-12 as a V1.1 reconciliation fix — see ROADMAP) | 3 workflows deployed |
| 5. Retention | 🟡 **Core retention infrastructure live** — System Health Monitor (11) + Client ROI Report (12) | 2 workflows deployed; named extras (review requests, referral invites, seasonal campaigns) remain unbuilt and are tracked as **post-V1, non-blocking** enhancements in `PRICING_PACKAGING.md`'s "Built for you" expansion menu — exactly the kind of revenue-expansion conversation `ONBOARDING_SOP.md` Phase 6 schedules for month 2-3, informed by a live client's actual needs rather than built speculatively in advance |

> **This table tracks workflow-build phases — it is not, by itself, a V1 launch-readiness signal.** As of 2026-06-08 (see `ROADMAP.md`'s dated entries), every commercial pillar required to responsibly acquire, onboard, deploy, support, and retain a paying client #1 has a real, tested, documented asset: `CLIENT_ACQUISITION_PLAYBOOK.md` (find them), `PRICING_PACKAGING.md` + `CLIENT_ONBOARDING_INTAKE.md` + `CLIENT_SERVICE_AGREEMENT_TEMPLATE.md` + `SMS_CONSENT_LANGUAGE_GUIDE.md` (close and onboard them), `CLIENT_DEPLOYMENT_GUIDE.md` + `CRM_SHEET_SCHEMA.md` + all 13 workflows now exported as real importable JSON (deploy for them), `CLIENT_WELCOME_GUIDE_TEMPLATE.md` (hand them something durable at go-live), Workflows 11/12 (support and retain them), and `CASE_STUDY_DATA_PLAN.md` (turn the relationship into the website's flagship proof asset). **The remaining named "not done" items across every list in this repo are explicitly non-blocking** (an attorney review of the contract template, full Phase-5 retention-extras, a branded intake form, a CRM migration, calendar sync) — each is a genuine post-V1 enhancement, not a precondition. The architect-level conclusion as of 2026-06-08: **the highest-leverage remaining action is no longer building — it's running the playbook in `CLIENT_ACQUISITION_PLAYBOOK.md` and landing client #1.**

### All live workflows in n8n (`valfin.app.n8n.cloud`)
| Workflow | n8n ID | Description |
|---|---|---|
| `01_crm_adapter_google_sheets` | `wVRHChyFrUNRaH4M` | CRM Adapter — only workflow that touches Google Sheets. Upserts leads, mints LEAD-####, logs to Communication Log. Accepts `followUpCount` for Phase 3 sequences. |
| `02_form_capture_scoring` | `HdJc5cy8cmqMBfGR` | Form Capture + Confirmation. n8n Form + webhook → CRM Adapter upserts the lead → Haiku 4.5 sends a confirmation SMS to the customer → logs the touch to Communication Log → Every Lead Alert (workflow 04) notifies the owner for every submission (email by default, SMS optional, off by default). No AI scoring — every lead follows the same path. |
| `03_missed_call_auto_sms` | `u9I1bqrLW6V5LtLp` | Missed-Call Auto-SMS. Twilio call-status → no-answer/busy filter → static SMS within seconds → CRM logs Comm entry only (no Lead). |
| `04_every_lead_alert` | `KIpMMKM8H5IZB9wb` | Every Lead Alert (formerly "Hot Lead Alert"). Sub-workflow called by 02 for **every** form submission — no scoring or branching. Emails the owner a clean, branded summary (business name, lead name, phone, email, requested service, requested appointment date/time, source, timestamp) by default; SMS is built but disabled by default. Toggles and contact info live in a `CONFIG` block (`EMAIL_ALERTS_ENABLED`, `SMS_ALERTS_ENABLED`, `OWNER_EMAIL`, `OWNER_PHONE` `+18575261499`). |
| `05_follow_up_sequence` | `chYfABnQdnPfiHQx` | Follow-Up Sequence. Daily 9 AM ET. Sends Day 1/3/7 SMS to New/Contacted leads, then updates status + Follow-up Count via CRM Adapter. Stops at 3 attempts. Booked leads auto-excluded. |
| `06_appointment_booking` | `ax2sMbvv0lqyJHMg` | Appointment Booking. Owner form → look up lead → write Appointments tab → customer confirmation SMS → update lead to Booked via CRM Adapter. Form: `https://valfin.app.n8n.cloud/form/eca6bfbb-ef53-4f82-b909-cbd2b818991a`. Structured scheduling: separate calendar-picker `date` field plus a fixed-slot `dropdown` (8 AM–5 PM, 30-minute increments, America/New_York) — no free text. Scheduling assumptions (`DEFAULT_TIMEZONE`, `BUSINESS_START_HOUR`, `BUSINESS_END_HOUR`, `APPOINTMENT_INCREMENT_MINUTES`) live in a `CONFIG` block for easy per-client overrides. **Tested end-to-end in production — confirmed working.** |
| `07_pipeline_status_digest` | `ehqNYjZRirX5L3sX` | Pipeline Status Digest. Daily 6 PM ET → reads all leads → tallies New/Contacted/Booked/Stale counts → lists Stale leads by name + phone → reports today's new leads/bookings → emails the owner a branded summary by default (SMS built but disabled by default). Read-only — no Sheets writes. Toggles and contact info live in a `CONFIG` block (owner phone configured `+18575261499`). |
| `08_weekly_pipeline_report` | `Y7ruzhYGMhE001fr` | Weekly Pipeline Report. Monday 8 AM ET → reads all leads → computes trailing-7-day metrics (new leads, bookings, stale count, bookings/new ratio, top sources) → emails the owner a branded weekly report by default (SMS built but disabled by default). Read-only. Owner contact info synced programmatically via `CONFIG` — zero manual setup. **Test-executed live (execution 54) — confirmed working.** |
| `09_appointment_reminders` | `bJcO5ox2u190bxTr` | Appointment Reminders. Hourly check → reads Appointments tab → computes 24h (20-28h out) and 2h (1-3h out) reminder windows using DST-safe Luxon `DateTime` math in `America/New_York` → personalized SMS per appointment → flags `Reminder 24h`/`Reminder 2h` columns to prevent duplicates. **Test-executed live (execution 55) — confirmed working** (correctly parsed/skipped a legacy unparseable test row with zero false-positive sends). |
| `10_reschedule_cancel` | `Bj5b3sUexa8EeQcK` | Reschedule/Cancel. Inbound SMS (Twilio Trigger) → classifies "reschedule"/"cancel" intent by keyword → finds the customer's upcoming Scheduled appointment by phone → updates Status (`Cancelled` on cancel, unchanged on reschedule) + appends a timestamped (`America/New_York`) Notes entry → replies to the customer (cancel: confirms + invites rebooking; reschedule: acknowledges + promises a callback) → alerts the owner by SMS to follow up. Irrelevant texts are silently ignored. Owner contact info and Twilio sender live in a `CONFIG` block. **Test-executed live via simulated inbound SMS (executions 63/64/65) — all three paths (reschedule found / cancel found / not-found) confirmed working.** |
| `11_system_health_monitor` | `U6t0b7M6lN8eA1JO` | System Health Monitor. Daily check (16:00 UTC) → reads live CRM data freshness (Appointments + Leads tabs) using DST-safe `America/New_York` math → flags overdue 24h/2h appointment reminders and stalled Day-1/3/7 follow-ups by exactly mirroring workflows 09's and 05's own "overdue" thresholds (plus safety buffers, so it only fires once their own definitions are violated with room to spare) → emails the *owner/operator* (not the client) one consolidated alert if anything looks stale, stays silent otherwise (SMS built but disabled by default). Alerts the vendor to a possibly-broken scheduled run *before* the client notices — retention-critical infrastructure. Deliberately checks business-outcome data freshness rather than n8n execution metadata (zero new credentials required, strictly larger failure-mode coverage). Toggles and contact info live in a `CONFIG` block. **Test-executed live (executions 75/76) — both the alert-firing path and the clean-pass zero-alert path confirmed working.** |
| `12_client_roi_report` | `ocAnTMCh068BxxXz` | Client ROI Report. Every 30 days (14:00 UTC) → reads Leads/Appointments/Communication Log → computes trailing-30-day metrics (new leads captured, missed calls recovered via instant text-back, appointments booked/kept) using DST-safe `America/New_York` math → emails the *client* (not an internal digest — addressed to their own brand name) a plain-language, outcome-framed recap closing with an open invitation to discuss the numbers (SMS built but disabled by default). Distinct in *purpose* from 07/08 (operational pipeline tools for the owner to act on day-to-day) — this is a recurring-fee-justification / renewal-and-expansion relationship tool. Deliberately written in industry-agnostic language (leads/appointments/missed-calls/follow-ups) so the same copy and metric set transfer to non-roofing clones with only a brand-name swap — see `docs/ROADMAP.md`'s 2026-06-08 entry for the "reusable framework lens" rationale. Client contact info and toggles live in a `CONFIG` block (`CLIENT_EMAIL`, `CLIENT_PHONE`, `WINDOW_DAYS: 30`). **Test-executed live (execution 85) — all computed metrics (new leads, missed-call recoveries, appointments booked, appointments kept) matched hand-verified expectations exactly.** |
| `13_appointment_reschedule_notifier` | `WzWw9vCYOCS6dSSS` | Appointment Reschedule Notifier. **Added 2026-06-12** — a V1.1 reconciliation fix for a real live-QA finding: when the owner manually changes a `Scheduled` appointment's `Appt Date`/`Appt Time` after the customer was already notified, the customer previously received no follow-up. On a schedule, reads the Appointments tab → compares `Appt Date`/`Appt Time` against `Notified Appt Date`/`Notified Appt Time` for every `Scheduled` row → on a mismatch, sends the customer a reschedule SMS (new date/time + invitation to reply or call if it doesn't work), emails the owner an alert (SMS optional, off by default), updates `Notified Appt Date`/`Notified Appt Time` to match, clears `Reminder 24h`/`Reminder 2h` so workflow 09 issues fresh reminders for the new time, and logs the SMS to Communication Log via the CRM Adapter. Rows where the values already match are skipped — this is the duplicate-notification guard. `America/New_York` default timezone, `CONFIG` block follows the V1.1 convention. **Test-executed live end-to-end (executions 297-299) — detection, customer SMS, owner email, sheet updates, Communication Log entry, and idempotent re-run all confirmed working.** |

**Credentials configured in n8n UI:** Google Sheets OAuth2 · Anthropic Header Auth · Twilio API. (Twilio account intentionally remains on trial/unverified status — a deliberate, paused, non-blocking external decision.)
See `docs/PROJECT_STATUS.md` for full workflow details and known issues.

**Running Valfin day-to-day, with a real prospect or client in front of you?** Start with `docs/FOUNDER_CLIENT_LIFECYCLE_PLAYBOOK.md` — added 2026-06-10, this is the single operational guide that maps the entire lifecycle (Prospect → Client → Renewal) to the specific document you need at each stage: discovery calls, demoing without exposing technical complexity, what the client should/shouldn't see, exactly what to collect from them, the founder kickoff checklist, how to use Claude to clone/configure a new client's deployment, the full onboarding/testing/go-live sequence, reporting and support cadence, expansion opportunities, and what's manual vs. automated and Valfin-owned vs. client-owned. Everything else below remains the canonical source for each individual stage — this is the map that ties them together.

**New here, or about to sell/deploy/run this for the first time?** Start with `docs/FOUNDER_TRAINING_PLAN.md` — a sequenced, ten-module curriculum that teaches the founder (not the engineer) how to explain the system, run discovery calls, demo it, price it, onboard and deploy a client, verify a deployment, customize it per-client, and handle support — in the deliberate order that makes each one easier because of the one before it. Created 2026-06-08, the day V1 was declared launch-ready (see `ROADMAP.md`): the natural next step once there's nothing left to *build* is making sure the person about to *sell, deploy, and run* this actually knows it cold.

**Looking for client #1 and don't have a prospect yet?** Start one step earlier, with `docs/CLIENT_ACQUISITION_PLAYBOOK.md` — the ICP, where to actually find Boston-area roofing prospects this week, first-contact scripts built around the "founding partner" framing (turning "no case study yet" into the single most differentiated thing you can say), and the discovery-call structure that hands directly into the runbook below. This was identified as the highest-leverage remaining gap in the 2026-06-08 architect-level platform review (see `ROADMAP.md`) — every other asset in this list assumes a prospect already exists; this is what gets one.

**On the call, and between "they said yes" and "they're a paying client"** — `docs/CLIENT_ACCEPTANCE_FLOW.md` is the master sequence, added 2026-06-10 to close the last gap in the founder-led journey (Interested Prospect → Discovery → Proposal → Agreement → Payment → Kickoff → Deployment):
- `docs/DISCOVERY_CALL_WORKBOOK.md` + `docs/DISCOVERY_CALL_NOTES_TEMPLATE.md` + `docs/DISCOVERY_CALL_SCORECARD.md` — the live, run-the-call script (talk track, calculator walkthrough, founding-partner offer, objection handling), a structured notes template to fill in during/after the call, and a scorecard for spotting patterns across your first 3-5 conversations
- `docs/PROPOSAL_PLAYBOOK.md` + `docs/CLIENT_PROPOSAL_TEMPLATE.docx` — how to fill out and send a brand-consistent proposal with that prospect's own numbers, bridging straight into the agreement
- `docs/PAYMENT_PROCESS.md` + `docs/STRIPE_SETUP_GUIDE.md` + `docs/INVOICE_TEMPLATE.docx` — the founder-stage payment design (Stripe Payment Links for the one-time setup fee and the recurring monthly fee), failed-payment handling, and the invoice template sent alongside each payment link

**Once a prospect says yes**, `docs/ONBOARDING_SOP.md` — the end-to-end runbook from "prospect says yes" to "client is live and supported," ties together:
- `docs/PRICING_PACKAGING.md` — internal pricing anchors, ROI framing, and contract structure (reconciled with the live website's "Foundation/Growth/Built for you" custom-pricing model — see that doc's reconciliation note)
- `docs/CLIENT_ONBOARDING_INTAKE.md` — the client-facing questionnaire that collects every configuration value up front, including the one-time-only baseline data needed for the flagship case study
- `docs/CLIENT_WELCOME_GUIDE_TEMPLATE.md` — the durable, leave-behind reference handed to the client at go-live (`ONBOARDING_SOP.md` Phase 5): what the system does for them in plain language, exactly what they'll receive and when (digests/reports/alerts), how to read their CRM sheet, what their own customers will experience, how to flag anything that looks off, and a week-1 FAQ — closing what the 2026-06-08 architect-level review identified as the highest-leverage remaining gap (a verbal-only walkthrough with nothing tangible left behind)
- `docs/CLIENT_DEPLOYMENT_GUIDE.md` — the master technical checklist of every per-client value across all 10 workflows, deployment order of operations, and a post-deploy verification plan
- `docs/CASE_STUDY_DATA_PLAN.md` — the spec for measuring and capturing the real before/after numbers the website's flagship case study (`website/src/content/results.ts`) is currently waiting on — the bridge between this track and the parallel website-build track
- `docs/CLIENT_SERVICE_AGREEMENT_TEMPLATE.md` — a fillable contract draft that closes the literal hard gate in `ONBOARDING_SOP.md` Phase 0 ("do not proceed without a signed agreement"); **read its header before using it** — it's a structured starting point for an attorney review, not a finished legal instrument
- `docs/SMS_CONSENT_LANGUAGE_GUIDE.md` — ready-to-hand consent-language recommendations for a client's digital forms, paper forms, phone scripts, and first-touch SMS, plus the "here's exactly how our opt-out handling works" talking points that double as a trust-building selling point in the sales conversation itself
- `docs/CRM_SHEET_SCHEMA.md` + `templates/Roofing_CRM_Google_Sheets_TEMPLATE.xlsx` — the CRM Google Sheet's full column-by-column schema (verified live for `Leads`/`Appointments`/`Communication Log`, clearly-flagged proposals for the five tabs no workflow has built against yet) and a ready-to-clone 8-tab spreadsheet template — closes a long-standing gap where the brief referenced a `Roofing_CRM_Google_Sheets.xlsx` that was never actually in the project folder

Together these are what turn "one custom build" into a repeatable, sellable offering.

> **Note on `website/`:** a Next.js marketing site lives in this repo under `website/` and is under **active independent development in a parallel session** (already shipped: scaffold, Lead Leak Calculator, all core pages, SEO). Don't edit its content from this track — coordinate via `docs/CASE_STUDY_DATA_PLAN.md` when real deployment data is ready to hand off.

## Models
- `claude-haiku-4-5` — instant customer-facing confirmation replies (volume/speed).
- All AI calls use `output_config.format` (JSON schema) so every node returns clean, parseable JSON.

> **V1.1 (2026-06-11):** AI lead scoring (`claude-sonnet-4-6` for Temperature/Lead Score/Hot-Lead classification) was removed system-wide. See `docs/V1_1_RECONCILIATION.md`.
