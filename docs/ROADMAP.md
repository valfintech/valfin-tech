# Roadmap
_Last updated: 2026-06-12 — Workflow 13 (Appointment Reschedule Notifier) added as a V1.1 reconciliation fix. See the entry immediately below for what changed and why._

> **2026-06-12 — Workflow 13 "Appointment Reschedule Notifier" added (V1.1 reconciliation fix, resumed on real QA signal):** During live QA testing, a real gap surfaced: a customer booked an inspection for Tuesday 10:00 AM via the website form; the owner later manually changed the appointment to Tuesday 1:00 PM (a normal scheduling-conflict resolution); the customer was never told. This is exactly the kind of "verified, concrete need" the 2026-06-10/11 pause called for as the bar to resume builder mode — not a speculative feature.
>
> **Audit first:** searched all 12 live workflows, the CRM schema, and Communication Log logic for any existing reschedule-notification mechanism. None existed — workflow 10 ("Reschedule/Cancel") only handles *customer-initiated* reschedule/cancel SMS replies; nothing detected an *owner-initiated* change to `Appt Date`/`Appt Time` on the Appointments sheet. Confirmed genuinely new gap, not a disconnected feature.
>
> **What was built (smallest safe addition):**
> - Two new trailing columns on `Appointments` — `Notified Appt Date` / `Notified Appt Time` — a snapshot of what the customer was last told. Workflow 06 (Appointment Booking) now seeds these equal to the initial `Appt Date`/`Appt Time` at booking time, so new appointments never look "rescheduled."
> - **Workflow 13 (`WzWw9vCYOCS6dSSS`)** runs on a schedule: for every `Scheduled` row, compares `Appt Date`/`Appt Time` against `Notified Appt Date`/`Notified Appt Time`. On a mismatch — i.e. the owner changed the appointment after the customer was notified — it sends the customer an SMS with the new date/time and an invitation to reply or call if it doesn't work, emails the owner an alert (SMS optional, off by default per the standard `CONFIG` dual-gate), updates `Notified Appt Date`/`Notified Appt Time` to match the new values, clears `Reminder 24h`/`Reminder 2h` so workflow 09 issues fresh reminders against the new time, and logs the outbound SMS to Communication Log via the CRM Adapter (workflow 01). Rows where the values already match are skipped entirely — the duplicate-notification guard.
> - `America/New_York` default timezone and the standard V1.1 `CONFIG` block convention (`COMPANY_NAME`, `OWNER_EMAIL`, `OWNER_PHONE`, `TWILIO_FROM_NUMBER`, `EMAIL_ALERTS_ENABLED: true`, `SMS_ALERTS_ENABLED: false`).
>
> **Tested end-to-end live** (executions 297-299): simulated an owner-initiated reschedule on a real Appointments row — confirmed detection, the exact required customer SMS text, the owner email alert, the owner SMS correctly *not* firing (toggle off), the `Notified Appt Date`/`Notified Appt Time` update, the `Reminder 24h`/`Reminder 2h` reset, the Communication Log entry (via CRM Adapter sub-execution), and — on an immediate second run with no further changes — zero duplicate notifications. Verified by static code review (no live side-effecting runs) that workflows 09 (Appointment Reminders) and 11 (System Health Monitor) read/write the Appointments sheet exclusively by column name and are unaffected by the two new trailing columns.
>
> Documentation updated: `CRM_SHEET_SCHEMA.md` (Appointments now 17 columns), `templates/build_crm_template.py` + regenerated `Roofing_CRM_Google_Sheets_TEMPLATE.xlsx`, and `README.md`'s workflow table/counts (12 → 13).
>
> **What this does not change:** the standing pause remains in effect. This was a reconciliation fix for a confirmed real-world gap in the already-shipped V1.1 system — not a resumption of speculative building. Future sessions should continue to build only on verified signal.

> **2026-06-11 — From conversation-based continuity to repository-based institutional knowledge:** Every prior pass in this roadmap — V1, then V1.1 — was completed by sessions that had the benefit of long-running conversation history: prior decisions, prior reasoning, prior context, all available in-thread. That's a real risk: any future session that starts fresh (a new chat, a different operator, a context-window reset) has historically had to either re-read a large portion of this repo from scratch or rely on the auto-memory system's necessarily-partial summaries.
>
> **What changed:** the founder asked for two permanent, repo-native operational assets that eliminate that dependency entirely, generated by reading and synthesizing the *entire* repository (docs, workflows, templates, prompts, website, exports) rather than relying on memory:
> - **`docs/VALFIN_MASTER_CONTEXT_PROMPT.md`** — the canonical onboarding document. What Valfin is, its mission, positioning (Revenue Operations Infrastructure, founder-led, radical honesty, roofing-as-V1/multi-industry-future), the V1 → V1.1 architectural evolution, a summary of all 12 workflows and every major doc/template/export and where it lives, the full founder-led lifecycle (sourcing → discovery → proposal → agreement → onboarding → deployment → go-live → support → renewal), known non-blocking open items, an explicit restatement that V1.1 is complete and builder mode is paused, and — critically — explicit instructions that future sessions must not proactively invent features, resuming only on verified signal from prospect conversations, onboarding, live delivery, support, retention, or expansion conversations. Closes with a "how to use this prompt" section mapping common future tasks (discovery prep, website updates, client deployment, marketing, support, vertical expansion, pricing review, workflow updates) to the right starting point.
> - **`docs/CLIENT_CLONING_MASTER_PROMPT.md`** — the operational deployment prompt. Self-sufficient with this document, a client's intake answers, and the repo: explains V1.1 and that roofing is the first validated implementation (not the final market); cleanly separates the reusable framework (workflow graphs, CRM Adapter pattern, CRM schema, dual-gate email/SMS pattern, `CONFIG` mechanism, Luxon/timezone pattern) from the industry-specific customization layer (`CONFIG` values, copy, forms, credentials, per-client CRM instance); documents all 12 workflows' mandatory/optional status, `CONFIG` presence, and email/SMS alert routing; the full intake-variable catalog (business identity, contacts, hours, lead handling, brand voice, existing tools, compliance, Sheets ownership, alert preferences, form customizations, reporting preferences); every account/credential/approval needed from signed-agreement through go-live; the exact ordered cloning sequence (Workflows 01 and 04 first, since every other workflow depends on their new instance IDs); per-workflow testing/validation procedures; and go-live, post-launch support, renewal/expansion, and final acceptance checklists. Closes with explicit guidance for cloning into other industries (HVAC, plumbing, dental, legal, etc.) — what changes (vocabulary, services, hours, job-value assumptions, occasionally CRM columns) and what never does (workflow graphs, CRM Adapter pattern, CONFIG mechanism, core logic).
>
> **Why now, and why this shape:** V1.1 (immediately below) was itself a simplification pass — removing complexity that had accumulated without proportional value. This entry is the same instinct applied one level up, to *how the project itself is operated across sessions*: rather than each future session re-deriving "what is Valfin, what's the state of things, how do I clone this" from scratch or from a memory summary, that knowledge now lives as two version-controlled, repo-native documents that are read, not recalled. `README.md` now points to both, prominently, as the first thing any new session should read — ahead of even `CLAUDE.md`'s original project brief, which describes the *first* build, not the system as it exists today.
>
> **What this does not change:** the standing pause from 2026-06-10/2026-06-11 (builder mode is paused; resume only on verified real-world signal) is unchanged and is itself restated inside `VALFIN_MASTER_CONTEXT_PROMPT.md` §14-15. Writing these two documents was an explicit, founder-directed documentation task — not a resumption of speculative building, and not an exception that future sessions should read as license to build ahead of demand. The next time either document goes stale (a 13th workflow, a second vertical, a real client #1) is itself one of the named real-world signals that would justify revisiting them.

> **2026-06-11 — V1.1 simplification and usability pass (resumed on real operational feedback):** After reviewing the live demo, workflows, Google Sheets, and founder experience end-to-end, AI lead scoring (`Lead Score`/`Temperature`/`Urgency`, Sonnet 4.6) was removed system-wide; "Hot Lead Alert" (04) was renamed **"Every Lead Alert"** and now fires for every submission (email by default, SMS built but disabled by default); workflows 07/08/11/12 converted from SMS-only to email-by-default reporting; appointment scheduling (06) now uses structured date/time-slot fields; all timestamps standardized on `America/New_York` via Luxon; and a centralized `CONFIG` block (email/SMS toggles, timezone, business hours, scheduling increments) was introduced across all notification/reporting workflows for easier per-client cloning. Treated as intentional product decisions, not bug fixes — see `docs/V1_1_RECONCILIATION.md` for the full changelog. Many entries below (Phase 2/3 "Delivered" tables, "Architectural Decisions") describe the pre-V1.1 system and are now historical except where annotated.

> **2026-06-10 — Final pre-pause pass: closing the gap between "interested prospect" and "paying client" — and pausing development:** Founder training (Module 2) was underway when the founder asked, one more time before pausing, to close the remaining practical gaps in the founder-led sales→delivery journey: **Interested Prospect → Discovery Conversation → Proposal → Agreement → Payment → Kickoff → Deployment.** This was the one part of that journey that had been *partially* covered (the discovery-call structure lived only inside `CLIENT_ACQUISITION_PLAYBOOK.md`'s sales-process section, and there was no proposal artifact, no payment process, and no document tying agreement → payment → kickoff together).
>
> **What was added, all on 2026-06-10:**
> - **Discovery call package** — `DISCOVERY_CALL_WORKBOOK.md` (the live, run-the-call script: talk track, calculator walkthrough, founding-partner offer, a Step-4 decision tree for every likely response, and a common-objections table), `DISCOVERY_CALL_NOTES_TEMPLATE.md` (structured per-call notes), and `DISCOVERY_CALL_SCORECARD.md` (fit-scoring + pattern review across the first 3-5 calls)
> - **Proposal system** — `CLIENT_PROPOSAL_TEMPLATE.docx` (a brand-consistent, Navy/Blue proposal document — cover page, situation, opportunity, solution, investment, founding-partner note, next steps) and `PROPOSAL_PLAYBOOK.md` (how to fill it out and send it within 24-48 hours of the call)
> - **Payment system** — `PAYMENT_PROCESS.md` (the founder-stage design: two Stripe Payment Links — a one-time setup fee that gates configuration, and a recurring monthly fee set up live at the go-live call; Smart Retries for failed recurring payments; what stays manual and why), `STRIPE_SETUP_GUIDE.md` (step-by-step account setup and per-client Payment Link creation — no prior Stripe integration existed), and `INVOICE_TEMPLATE.docx` (sent alongside each Payment Link)
> - **`CLIENT_ACCEPTANCE_FLOW.md`** — the connective document tying proposal → agreement → payment → kickoff into one sequence, naming the exact hard gate (setup-fee payment confirmed in Stripe) before onboarding begins
>
> Existing documents were updated to cross-reference these: `ONBOARDING_SOP.md` Phase 0 and Phase 5, `VALFIN_FOUNDER_OPERATING_MANUAL.md` §7-8 and its Gaps table, `FIRST_CLIENT_READINESS_REPORT.md`, and `README.md`'s documentation chain. The two new `.docx` templates were force-added to git (alongside the rest of `docs/*.docx`/`*.pptx`, which are gitignored by pattern but intentionally tracked as real deliverables).
>
> **Reassessment: no material gaps remain between "interested prospect" and "paying client."** Every transition in the founder-led journey now has a named document, template, or script, and every document hands off cleanly to the next. The only open items are pre-existing, already tracked, and none are missing assets: attorney review of the service agreement (a human-only action), and creating the live Stripe account (`STRIPE_SETUP_GUIDE.md` §1-2 — a one-time ~15-minute task, now newly tracked in `FIRST_CLIENT_READINESS_REPORT.md`).
>
> **Valfin Version 1 is complete.** This marks the formal end of the builder-mode track for V1: no further documents, workflows, or infrastructure should be built speculatively. The project now switches **permanently out of builder mode** until real-world signal — an actual discovery call, a sent proposal, a signed agreement, an onboarding in progress, or a live client's feedback — surfaces a verified, concrete need. At that point, the relevant document above is the one to open and revise; this is not a dead end, it's the system waiting on the input only the real world can provide.

> **2026-06-08 — Mode shift: from architect/builder mode to founder training mode (thirteenth pass, same day):** Once V1 was declared launch-ready (see the conclusion entry immediately below — reached the same day, after three consecutive architect-mode gap-closing passes), the next instruction was a genuine pivot, not a continuation: *"Switch from builder mode to founder training mode."* This is worth naming explicitly as a distinct kind of session, because it's the first time this project has asked for something that isn't "build/fix/document an asset" — it's "teach the human who's about to run this business how to actually run it." That's a different deliverable with a different shape: not a technical guide, not a per-client template, but a **sequenced curriculum** that takes the founder from "I oversaw this getting built" to "I can confidently sell, deploy, manage, and support it" — in an order where each skill makes the next one easier, not a flat checklist to absorb in any sequence.
>
> **`docs/FOUNDER_TRAINING_PLAN.md`** is the result — ten modules, deliberately *re-sequenced* from the order they were requested in, because the highest-leverage starting point isn't "explain the system to prospects" (the first item on the list) but **understanding the system as one continuous story** (originally listed *last*, as item 10). The reasoning, made explicit in the document itself: you cannot convincingly explain, sell, demo, price, or support something you don't have a clear mental model of — and explaining/selling/demoing are *the same underlying story, told to three different audiences, in three different formats*. Build that story once, solidly, and modules 2 through 4 stop being three things to learn and become one thing to retell. From there the sequence simply mirrors the real chronological arc of the business — pitch → discover → demo → price → onboard → deploy → verify → customize → support — because that arc *is* the natural learning progression: each stage hands the next one exactly the context it needs.
>
> **What makes this different from every other companion doc in the repo:** every other doc in this project teaches *what to do* (a runbook, a checklist, a fillable template). This one teaches *how to think* — and it does that by pointing, deliberately, at the existing canonical sources (`CLIENT_ACQUISITION_PLAYBOOK.md`, `PRICING_PACKAGING.md`, `ONBOARDING_SOP.md`, `CLIENT_DEPLOYMENT_GUIDE.md`, `CLIENT_WELCOME_GUIDE_TEMPLATE.md`) rather than rehashing them — avoiding exactly the kind of "now there are two slightly-different versions of the same instructions" drift this project has been careful to avoid all along. The **one** place it teaches directly rather than points is Module 1, because no existing document narrates the system end-to-end at founder altitude — the README's workflow table and `PROJECT_STATUS.md` are accurate but inventory-shaped (a list of twelve things), not story-shaped (one continuous arc a prospect can follow). Module 1 supplies that missing narrative explicitly: "a single lead's journey, start to finish," walking through all twelve workflows in the natural chronological order a business owner would actually encounter them — Saturday's missed call → Sunday's form submission and AI scoring → the instant hot-lead alert → the booking → the daily digest → the weekly report → the reminders → the reschedule handling → the quiet follow-up sequence for leads that don't book → and, finally, the two workflows nobody but the operator and the client ever sees directly (the health monitor watching the operator's back, and the ROI report proving the system's worth to the client every 30 days). The document then explicitly shows how that same story *becomes* the demo script (Module 4) and the explanation framework (Module 2) — turning what could have been three separate things to memorize into one thing to internalize and retell.
>
> **Per the standing reusability lens**, every module names not just *what* to learn but *how to know you've actually learned it* — a concrete practice exercise and an honest "mastery check" — because reading about running a discovery call and being able to run one cold are very different competencies, and conflating them is exactly the kind of gap that would otherwise only surface in front of a real prospect. A pacing guide closes the document (roughly an eight-day sequence, deliberately not a "read it all today" binge — the practice exercises need room to breathe between sessions) and ends, on purpose, by pointing the founder *back out* of the document entirely: **"stop training and go run Module 3 for real... the fastest way to find out what you still don't know is to have it."** That closing line is the connective tissue between this document and the platform's actual highest-leverage next action (landing client #1, per the conclusion entry below) — training exists to make that conversation go well, not to defer it.
>
> Cross-linked from `README.md` as the very first companion-doc pointer — ahead of even the acquisition playbook — framed explicitly as "the natural next step once there's nothing left to *build*."

> **2026-06-08 — Architect-level conclusion: V1 launch-readiness review — no material launch blockers remain (twelfth pass, same day):** Asked, for a third consecutive time today, to re-evaluate the whole platform — including "framework architecture" as an explicitly-named category — identify the single highest-leverage remaining gap, close it, and **stop only for a genuine blocker, or upon concluding no material V1 launch blockers remain and the project has reached a launch-ready state.** This pass is that conclusion, reached deliberately rather than by default — it would have been easy to keep finding small things to polish indefinitely; the harder and more honest architect-level judgment call is recognizing the point past which more building stops being the highest-leverage move.
>
> **The re-scan, one more time, against "would this stop me from confidently signing and deploying client #1 tomorrow":**
> - **Find them** → `CLIENT_ACQUISITION_PLAYBOOK.md` — ICP, sourcing channels, scripts, discovery-call structure, prospect tracker. ✅
> - **Close & onboard them** → `PRICING_PACKAGING.md`, `CLIENT_ONBOARDING_INTAKE.md`, `CLIENT_SERVICE_AGREEMENT_TEMPLATE.md`, `SMS_CONSENT_LANGUAGE_GUIDE.md`. ✅ (one sub-item — attorney review of the contract template — is correctly tracked as the kind of thing that *cannot* be completed inside an AI session; it's a real open item, but it's a human-action item, not a missing asset)
> - **Deploy for them** → `CLIENT_DEPLOYMENT_GUIDE.md`, `CRM_SHEET_SCHEMA.md` + clone-ready `.xlsx`, and — as of this pass — **all 12 live workflows have real, valid, convention-matching importable JSON exports** in `workflows/`. ✅
> - **Verify it before they see it** → §5 post-deploy checklist, the exact `test_workflow`/`get_execution` pattern proven across every workflow in this build. ✅
> - **Hand them something durable at go-live** → `CLIENT_WELCOME_GUIDE_TEMPLATE.md` (closed earlier today). ✅
> - **Support & retain them** → Workflow 11 (System Health Monitor — alerts the *operator* before the client ever notices a silent failure) + Workflow 12 (Client ROI Report — proves the system's value to the *client* every 30 days, in their own brand voice) + `ONBOARDING_SOP.md` Phase 6's full ongoing-support runbook. ✅
> - **Prove it and expand later** → `CASE_STUDY_DATA_PLAN.md` (the bridge to the website's flagship proof asset) + `PRICING_PACKAGING.md`'s "Built for you" expansion menu. ✅
> - **Compliance** → `SMS_CONSENT_LANGUAGE_GUIDE.md` + the live, tested opt-out fix (`PROJECT_AUDIT.md`, executions 67/68). ✅
>
> **On "framework architecture" specifically** (asked about by name in two consecutive passes — worth addressing head-on rather than waving past): the reusable-framework lens is *consistently and concretely applied* — every recent asset (the acquisition playbook's ICP, Workflow 12's industry-agnostic metrics and copy, the welcome guide's bracketed template structure, the deployment guide's §3e business-rule-constants table that already flags "Valfin-specific judgment calls, not universal truths") carries its own explicit "what generalizes and how" reasoning, in place, next to the thing it describes. A genuinely real but *smaller and non-blocking* observation surfaced here: there is no single consolidated "framework architecture" document that names the reusable engine (CRM-adapter pattern, AI scoring/response pipeline, scheduling/reminder/reporting stack) versus the per-vertical surface layer (prompts, SMS copy, business-rule constants) in one place, the technical counterpart to what the acquisition playbook did for the *sales* side of multi-vertical expansion. **This is correctly a V2/expansion-readiness item, not a V1 launch blocker** — and there's a sharper reason to defer it than just "it's not urgent": writing it *now*, before a single real clone has ever been attempted, means writing it from theory. Writing it *after* client #2 (whether same-vertical or the first cross-vertical clone) means writing it from one real, lived experience of exactly which assumptions held and which didn't — a dramatically better-grounded document for the same effort, later. Building it now would be exactly the kind of pre-emptive infrastructure investment the existing Phase-6 guidance already warns against ("expansion-revenue territory, not a Day-1 pitch" — the same logic applies to expansion *infrastructure*, not just expansion *revenue*).
>
> **The conclusion:** every named gap surfaced across all of today's architect-mode passes — acquisition (closed), client-facing onboarding collateral (closed), deployment-asset integrity (closed) — is now closed, tested where testable, cross-linked into the documentation chain, and committed/pushed. Every remaining "open item" anywhere in this repo is explicitly, individually marked as non-blocking, and each one falls into exactly one of three honest categories: **(a) requires a human, not more AI session time** (the attorney review — the one item that *cannot* be closed this way, by its own nature), **(b) is better built from real post-launch experience than speculation** (Phase-5 retention extras, a consolidated framework-architecture doc, GHL migration, calendar sync), or **(c) is a deliberate, explicitly-confirmed non-blocking external/business decision** (Twilio trial-status, branded-form polish). None of the three categories describes a "missing asset that blocks responsibly taking on client #1." **The project has reached a true Version 1 launch-ready state.** The highest-leverage action from here is no longer architectural — it's operational: open `docs/CLIENT_ACQUISITION_PLAYBOOK.md`, run its "this week" checklist (list 10 names, make 5-10 mystery-customer calls, start the tracker), and have the first real conversation. Every subsequent architect-mode pass should now be triggered by — and grounded in — what that conversation, and the live client #1 deployment that follows it, actually reveals; that real-world signal is the one input this session genuinely cannot manufacture on its own, and it will surface a sharper, better-evidenced set of "next gaps" than another theoretical pass over the same documentation ever could.

> **2026-06-08 — Architect-level re-evaluation: closing the last named integrity gap — exporting workflows 11 & 12 as real, importable JSON (eleventh pass, same day):** Asked once more to re-evaluate the whole platform — including, this time, "framework architecture" as an explicit category — and to close *the* single highest-leverage remaining gap, stopping only for a genuine blocker or a conclusion that **no material V1 launch blockers remain**.
>
> **The re-scan:** confirmed `docs/CLIENT_ACQUISITION_PLAYBOOK.md` and `docs/CLIENT_WELCOME_GUIDE_TEMPLATE.md` (this same day's two prior closes) have fully closed gaps #1 and #2. Checked "framework architecture" specifically — found the reusability lens is *consistently applied* (every recent asset carries an explicit "how this generalizes" section: the acquisition playbook's ICP, Workflow 12's industry-agnostic copy and constants, the welcome guide's bracketed-template structure), so a *consolidated* framework-architecture document would mostly restate what's already said in-place near the code/copy it describes — a smaller, more cosmetic gap than a genuinely missing capability. The one item that *did* surface as a real, concrete, still-open integrity gap was **#3 from the 2026-06-08 ranking, never closed**: `ls workflows/` showed `01`–`10` each have a clone-ready `.json` export, workflow 11 existed only as a `.ts` SDK-source file, and workflow 12 had **nothing** committed to `workflows/` at all — while `CLIENT_DEPLOYMENT_GUIDE.md` §4 step 6 has, this entire time, instructed the operator to **"Import workflows 07, 08, 09, 11, 12."** That instruction was, until this pass, simply impossible to follow using the repo's own assets for two of the five named workflows.
>
> **Why this ranked highest of what remained:** it's the same *species* of gap as the missing-CRM-template issue closed two passes ago — a documented operational instruction pointing at a file that doesn't exist — and those are uniquely dangerous because they're invisible until the exact moment someone tries to follow them, which (by design) is mid-deployment for client #2, under time pressure, with a paying relationship on the line. A framework that can't actually be cloned via its own documented process isn't really a portable framework yet, no matter how cleanly the *running* instance is built — and "Roofing remains the first deployment template, not the final market" is an empty promise if the template itself can't be exported. Closing it directly serves the "framework architecture" lens the user named this pass: it's the literal mechanical step that turns one bespoke live build into a clonable artifact.
>
> **How it was closed:** rather than trying to hand-reconstruct the JSON (error-prone and exactly the kind of drift this gap-species describes), pulled the live, currently-published definitions straight from the production n8n instance via `get_workflow_details` for both `U6t0b7M6lN8eA1JO` (Workflow 11) and `ocAnTMCh068BxxXz` (Workflow 12), stripped the API-only metadata fields (`id`, `versionId`, `createdAt`, `tags`, `meta`, etc.) that the existing `01`–`10` exports don't carry, and wrote `workflows/11_system_health_monitor.json` (8 nodes) and `workflows/12_client_roi_report.json` (10 nodes) in the same `name`/`active`/`settings`/`nodes`/`connections` shape as every existing export. One real discrepancy surfaced and was fixed before calling it done: the freshly-exported nodes lacked the `credentials` placeholder blocks (`{id: '', name: 'Twilio account'}` / `{id: '', name: 'Google Sheets account'}`) that every Twilio/Google-Sheets node in `01`–`10`'s exports carries — exactly the convention `CLIENT_DEPLOYMENT_GUIDE.md` §2 depends on for "create the 3 credentials, then re-point every reference." Patched all 7 affected nodes across both files (3 in workflow 11, 4 in workflow 12) to match that convention precisely, then re-validated both as clean JSON. The retained `11_system_health_monitor.ts` SDK-source file was deliberately left in place rather than deleted — it carries the workflow's original design-rationale comments (the "why 16:00 UTC, why these specific overdue thresholds" reasoning), which is real documentation value the `.json` export doesn't preserve; `README.md`'s folder-layout note now explains its presence so a future reader doesn't mistake it for an importable artifact or wonder why `workflows/` isn't 100% `.json`. **All 12 live workflows now have real, valid, convention-matching importable exports — `CLIENT_DEPLOYMENT_GUIDE.md` §4 step 6 can finally be followed exactly as written, end to end, using nothing but this repo's own checked-in assets.**
>
> **Where this leaves V1 launch-readiness:** every gap identified across three consecutive architect-mode passes today — acquisition (no path to client #1), client-facing onboarding collateral (nothing durable handed over at go-live), and deployment-asset integrity (instructions pointing at files that didn't exist) — is now closed, tested where testable, and cross-linked into the existing documentation chain. The remaining items in every "Open Items" / "Optional Enhancements" list across the docs are explicitly marked non-blocking (an attorney review of the service-agreement template, Phase-5 retention workflows, a branded intake form, GoHighLevel migration, calendar sync) — each is genuinely a *post-V1* enhancement, not a precondition for safely and credibly taking on client #1. **No further re-scan this pass surfaced a new gap that outranks "go run the playbook and get client #1 signed."** That is itself a meaningful finding worth stating plainly: the platform has reached a state where the highest-leverage remaining action is no longer *building* anything — it's *using* what's been built. The next architect-mode pass should treat "client #1 is live" as the trigger for the next real wave of gaps (the kind that can only be seen once real usage exists), not assume more pre-emptive infrastructure is the answer.

> **2026-06-08 — Architect-level re-evaluation: re-ranking the remaining gaps to true V1 launch-readiness, and closing the new highest-leverage one (tenth pass, same day):** Asked again to step back into architect/product-owner mode — this time explicitly to **re-evaluate** (not simply continue down a prior list) across an even wider net: platform, website, deployment/onboarding/**acquisition**/support/**retention** assets, and documentation, and to identify *the single* highest-leverage remaining gap (not a ranked top-3 — one). This was a genuine re-scan, not a rubber stamp: it specifically checked whether closing gap #1 (the acquisition playbook, closed earlier today) had changed the picture, and whether the user's newly-named categories ("acquisition assets," "retention assets") surfaced anything not previously seen.
>
> **What the re-scan found:** the two newly-named categories both check out as adequately covered — *acquisition* by the brand-new `CLIENT_ACQUISITION_PLAYBOOK.md`, and *retention* by the combination of Workflow 12 (Client ROI Report), Workflow 11 (System Health Monitor), `ONBOARDING_SOP.md` Phase 6, and `PRICING_PACKAGING.md`'s "Built for you" expansion menu — genuinely a strong, multi-layered pillar already. No *new* gap outranked the prior #2 candidate. A targeted grep across `docs/*.md` for any existing "welcome packet / welcome guide / client-facing guide / getting started / how-to-use / troubleshooting / support contact / FAQ / leave-behind / one-pager" material returned **zero genuine hits** — and a re-read of `ONBOARDING_SOP.md` Phase 5 confirmed it is, word for word, still just: *"Schedule a short walkthrough call... show them... read the actual SMS scripts aloud... [and] give them a direct way to report anything that looks off."* Nothing durable. Nothing the client keeps. Nothing their staff can be handed. Every other relationship-facing asset in the platform — the deployment guide, the intake packet, the consent-language guide, even the brand-new acquisition playbook — produces something tangible that outlives the conversation that created it. Phase 5, the literal moment a paying client first experiences "is this a real, professionally-run thing or an improvised one," produces nothing. **That gap — confirmed real, confirmed unaddressed, confirmed unclaimed by any other document — is the single highest-leverage remaining item**, for the same reason gap #1 (acquisition) ranked first last pass: it sits at a pivot point everything downstream depends on. A client who walks away from go-live with nothing tangible starts the relationship one notch more skeptical, asks more avoidable questions in week 2 (raising support load right when the operator should be focused on the *next* prospect from the freshly-closed acquisition gap), and has a measurably weaker foundation for the renewal conversation `PRICING_PACKAGING.md` and Workflow 12 already set up to win.
>
> **Closed it this pass: `docs/CLIENT_WELCOME_GUIDE_TEMPLATE.md`** — a new fillable, plain-language, leave-behind reference document for the client, structured in the same honest, non-corporate voice already established across `CLIENT_ACQUISITION_PLAYBOOK.md` and the live website (no jargon, no "leverage synergies," just "here's exactly what this does for you and when you'll hear from it"). Seven sections: (1) what the system does for the client's business in plain outcome language; (2) a table of exactly what they'll receive and when — instant urgent-lead alerts, daily digest, weekly report, and the **monthly ROI recap** (translating workflows 04/07/08/12's outputs into "here's what you'll see and when" client-facing language for the first time anywhere in the repo); (3) how to read their own CRM sheet (Hot/Warm/Cold, status flow, follow-up counts — deliberately kept in sync with `CRM_SHEET_SCHEMA.md`'s verified live vocabulary); (4) what their *own customers* will experience at each touchpoint, with an operator note to read 2–3 actual brand-voice-rewritten SMS scripts aloud together; (5) "if something looks off" — turning the previously-verbal Phase 5 step 3 into a durable, calm, no-blame channel; (6) a week-1 FAQ answering the six questions every new client asks in the first seven days ("how do I know it's working," "can I change the hours," "is this replacing my team," etc.); (7) a short closing note framing the system as something that gets sharper over time, not a frozen one-time install. **Per the reusability lens**, every section is written in industry-agnostic vocabulary — "leads/appointments/your customers/your team," never "roofs/shingles/storm damage" — with bracketed per-client placeholders and a single clearly-marked example section, mirroring the exact template pattern already proven by `CLIENT_SERVICE_AGREEMENT_TEMPLATE.md` and the structural lens applied to `CLIENT_ACQUISITION_PLAYBOOK.md`'s ICP section; an operator checklist at the foot of the document makes the per-client customization steps explicit and impossible to skip silently. Cross-linked into `ONBOARDING_SOP.md` Phase 5 (which now instructs the operator to fill it in *from the existing intake answers* — described as "minutes, not hours" — and **read it aloud together with the client**, then leave the filled-in copy behind, converting three previously-verbal talking points into one durable artifact) and its Open Items table (new closed row), and into `README.md`'s companion-doc list. **This is the client-facing analog of what the acquisition playbook did for the *start* of the relationship — it gives the *handoff* moment the same "here's exactly what to say and leave behind" treatment**, closing the loop on the only remaining pivot-point in the client lifecycle that produced nothing tangible.

> **2026-06-08 — Architect-level platform review: ranking the top 3 remaining gaps to true V1 launch-readiness, and closing the highest-leverage one (ninth pass, same day):** Asked to step back from workflow-building and operate as architect/product-owner — evaluate the *whole* platform (system, website, deployment/onboarding/monitoring/reporting/client-readiness assets) against the bar of "capable of acquiring, onboarding, deploying, supporting, retaining, and expanding paying clients," identify the top 3 remaining gaps, rank them by business impact, and close the highest one first. Surveyed the full asset inventory: 12 live/tested workflows, a complete deployment guide + per-client config catalog, a phase-by-phase onboarding SOP, pricing/packaging, a client intake questionnaire, a service-agreement template, a TCPA consent guide, a CRM schema + clone-ready template, automated system-health monitoring, and an automated client-facing ROI report — genuinely comprehensive infrastructure for *running* a client relationship once one exists. Then checked the website (`website/src/content/`) to see how the parallel track is currently positioning the business, both to stay consistent and to avoid duplicating it.
>
> **The top 3 gaps identified, ranked by business impact:**
> 1. **🥇 No documented path to finding client #1 at all (closed this pass).** Every commercial asset in the repo — `PRICING_PACKAGING.md`, `CLIENT_ONBOARDING_INTAKE.md`, `ONBOARDING_SOP.md`, the service agreement, all 12 workflows — picks up at the exact instant `ONBOARDING_SOP.md` Phase 0 begins: *"Trigger: Prospect agrees to move forward."* Nothing in the repo, and nothing on the website (which is built to *convert* visitors who already arrived, not generate them), answers the more basic question: **how does a prospect arrive at "agrees to move forward" in the first place, when there's no inbound traffic, no search ranking, and no published case study yet?** This is the literal bottleneck in front of every other piece of infrastructure — a fully-tested 12-workflow stack and a polished website both sit completely idle without it. It ranks #1 because it is the actual precondition for all five of the other named pillars (onboard/deploy/support/retain/expand are all *downstream* of acquire) — and because, on inspection, it was the easiest gap to leave undocumented precisely *because* it isn't a workflow or a template; it's a plan for a human being to have conversations with other human beings, and that's exactly the kind of gap that survives a dozen "what's the next workflow to build" passes untouched.
> 2. **🥈 No durable client-facing "welcome" reference material for go-live.** `ONBOARDING_SOP.md` Phase 5 currently hands the client nothing more than a verbal walkthrough call — no leave-behind document explaining what the system does, what to expect in week 1, who to contact, or how to read their own CRM/digests/ROI reports. This is a real but smaller gap than #1: it affects the *quality* of an already-acquired relationship (lower early support load, more "feels professionally run, not improvised" trust, a stronger foundation for the renewal conversation `PRICING_PACKAGING.md` and Workflow 12 already set up) — but it can't matter at all until there's a client to hand it to. Tracked here as the next item to close.
> 3. **🥉 Workflows 11 and 12 have no importable JSON export checked into the repo.** Every workflow 01–10 has a clone-ready `.json` export in `workflows/`, matching `phase2_setup.md`'s "Workflows → Import from File" instructions and `CLIENT_DEPLOYMENT_GUIDE.md` §4's "Import workflows 07, 08, 09, 11, 12" step — but workflow 11 exists in the repo only as a `.ts` SDK source file (not an importable export), and workflow 12 has nothing committed at all. This is exactly the same *species* of gap as the missing-CRM-template issue closed earlier today: a deployment-guide instruction that can't actually be followed using the repo's own assets, the kind of silent integrity gap that would only surface — painfully — mid-deployment for client #2. It ranks #3 because it doesn't block client #1 (that deployment is direct configuration of the already-live n8n instance, not a from-repo clone) — but it's cheap to close and worth fixing before it's forgotten.
>
> **Closed gap #1 this pass: `docs/CLIENT_ACQUISITION_PLAYBOOK.md`** — a new companion doc, cross-linked from `README.md` and `ONBOARDING_SOP.md` Phase 0, that covers: a generalized Ideal-Customer-Profile filter (deliberately mirroring the website's own "who this is for" list — Roofing/HVAC/Plumbing/Electrical/Solar/Contractors/Real Estate/Insurance/Legal/Dental/Med Spas/Consulting — rather than inventing a different filter for outreach than the one already promised to visitors) plus a concrete, this-week-doable starting point (Boston-area roofing, with five ranked sourcing channels from warmest to coldest, and a "mystery customer" exercise that turns a cold list into a warm, evidence-backed one); first-contact scripts for warm-introduction, cold-outreach, and in-person channels, all built around a **"founding partner" reframe** that turns "you have no case study yet" — the single most obvious objection — into the single most differentiated thing to say in the room, deliberately written to *mirror, not contradict*, the website's own radical-honesty trust positioning ("We'd rather show you one real result than tell you about a hundred imaginary ones" / the in-progress case-study page that says outright "there's no number here yet, and here's exactly why"); a 15-minute discovery-call structure that explicitly hands off into the Lead Leak Calculator and `PRICING_PACKAGING.md`'s existing sales guidance (rather than reinventing either); a lightweight prospect tracker deliberately modeled on the live CRM's own `Leads` tab shape (eating the product's own cooking); and — per the standing reusability directive — an explicit "how this generalizes" section spelling out that expanding to HVAC/plumbing later means re-running this *exact* playbook with three swapped inputs (vertical name, sourcing-channel equivalents, and an upgraded founding-partner pitch once a second verified case study exists), not writing a new one from scratch. **This single document converts "we have a great system and no way to find out if anyone wants it" into "here's exactly who to call this week and what to say"** — which is, by a wide margin, the highest-leverage thing this platform was missing.
>
> **2026-06-08 — Building Workflow 12, Client ROI Report, AND introducing the "reusable framework" lens (eighth pass, same day):** The user reaffirmed the standing priority order (client support readiness, system-health monitoring, TCPA/compliance, ROI reporting, retention/expansion — closed/closed/closed/**now closed**/ongoing) **and** layered a brand-new strategic clarification on top of it: roofing is "Version 1 of the framework, not the final market" — the long-term goal is a *reusable service-business automation framework* portable to HVAC, plumbing, dental, med spa, legal, and other lead/appointment-driven businesses. The instruction was explicit that this should change *how* future work gets done (favor industry-agnostic patterns in architecture/docs/deployment/monitoring/reporting/ops "whenever practical") without changing *what* gets prioritized first (still: land roofing client #1). This build was the perfect single move to honor both directives at once — it was already next in line as the last remaining named workflow-shaped Open Item, *and* it was the natural place to first apply the new lens, since a client-facing report's language is exactly the kind of thing that either locks a system into one vertical's vocabulary or doesn't. **`12_client_roi_report`** (`ocAnTMCh068BxxXz`) is now live and tested (execution 85): every 30 days it reads Leads/Appointments/Communication Log, computes trailing-window metrics (new leads captured, hot/urgent leads flagged instantly, missed calls recovered via instant text-back, appointments booked vs. kept), and texts the *client* — not an internal digest, but a relationship/retention-framed recap addressed to their own brand name, closing with an open invitation to discuss the math. This is deliberately distinct in *purpose*, not just audience, from workflows 07/08 (which are operational pipeline tools the owner uses to run their business day-to-day) — 12 exists to answer "is this system worth what I'm paying for it," the literal question that determines contract renewal. **Applying the reusability lens concretely:** every metric name, every line of SMS copy, and every constant in the build was chosen to generalize — "leads captured," "appointments booked/kept," "missed calls recovered," "follow-ups sent" are universal service-business vocabulary (a dental practice, an HVAC company, and a law firm all have leads, appointments, missed calls, and follow-ups; none of the copy says "roof," "shingle," or "storm damage"). The cadence (`daysInterval: 30` rather than calendar-month math) was chosen specifically because "every 30 days" is a simpler, edge-case-free, and equally-explainable line to any client regardless of industry. The one new constant introduced, `REPORT_WINDOW_DAYS`, deliberately drives *both* the schedule cadence and the metric-window math from a single source — change the cadence once, and the reported numbers automatically describe the same window, in any clone. And rather than minting a new `CLIENT_PHONE` constant for what is conceptually "the client," the build deliberately reuses the existing `OWNER_PHONE` convention (it's the same recipient as 07/08/10/11 — the business owner *is* the client in this single-owner-operator model that roofing/HVAC/plumbing/dental/legal SMBs overwhelmingly share) — fewer uniquely-named per-client constants means faster, less error-prone cloning. Tested live with hand-crafted pinned data spanning both inside and outside the 30-day window boundary across all three source tabs simultaneously — all five computed metrics (`newLeads: 3`, `hotOrUrgentLeads: 1`, `appointmentsBooked: 3`, `appointmentsKept: 2`, `missedCallsRecovered: 2`) matched hand-verified expectations exactly, and the rendered SMS read naturally. Documented across `README.md`, `PROJECT_STATUS.md`, `CLIENT_DEPLOYMENT_GUIDE.md` (§3 config catalog incl. the new `REPORT_WINDOW_DAYS` row, §3d schedule table, §4 deployment order, §5 verification checklist, §6 now marks this "done"), and `ONBOARDING_SOP.md` (Phase 6 step 2 + Open Items table, now closed). **Both previously-open workflow-shaped Open Items (system-health monitoring, client-facing ROI report) are now closed** — the highest-leverage *named* gaps toward landing-and-retaining client #1 have all been built, tested, and shipped. What remains per the priority order is ongoing: ordinary retention/expansion-readiness work (Phase 5), and continuing to apply the new reusability lens as a standing consideration on every future build — not a separate project, but a filter on how this one gets built out.
>
> **2026-06-08 — Closing the missing-CRM-template gap (seventh pass, same day):** Asked to search the project for any reference to `Roofing_CRM_Google_Sheets.xlsx` / the CRM schema and, if missing, generate what's needed so the project doesn't depend on lost prior conversations. Confirmed via the earliest project memory (`roofing-phase2-decisions`, 2026-06-05) that the brief always referenced this file but **it was never actually present in the project folder** — the early build proceeded against "the column spec in the brief" from memory, and no copy of that spec, nor any `.xlsx`/`.csv` template, has existed in the repo since. That's a real gap: `CLIENT_DEPLOYMENT_GUIDE.md` §1 has told every future cloning session to go get "a copy of the CRM Google Sheet template" — pointing at something that doesn't exist. Closed it two ways: (1) **`docs/CRM_SHEET_SCHEMA.md`** — re-derives the exact live schema for the three tabs real workflows actually touch (`Leads`, `Appointments`, `Communication Log`) directly from current production workflow source (not from memory — e.g. `Appointments`'s 15-column list was extracted fresh from workflow 06's `Write Appointment` node, since no doc had ever spelled it out in full), and separately proposes — clearly flagged as **reconstructed, not verified** — starting schemas for the five brief-named tabs no live workflow has ever written to (`Quotes`, `Jobs`, `Follow Ups`, `Team Schedule`, `Dashboard`); (2) **`templates/Roofing_CRM_Google_Sheets_TEMPLATE.xlsx`** (+ its build script) — a ready-to-clone 8-tab spreadsheet with correct headers, frozen panes, and clearly-marked example rows, finally giving `CLIENT_DEPLOYMENT_GUIDE.md` §1 something real to point a client-#2 deployment at. Cross-linked into `CLIENT_DEPLOYMENT_GUIDE.md` §1, `phase2_setup.md` §0, and `README.md`. This wasn't on the user's named priority list, but it's exactly the kind of "future session has nothing to stand on" gap that the standing directive (treat the repo as source of truth, don't depend on conversations that may not survive) exists to catch — and it directly de-risks the very next client deployment.
>
> **2026-06-08 — Building Workflow 11, System Health Monitor (sixth pass):** Continuing the user's named priority order (client support readiness, system-health monitoring, TCPA/compliance — closed, ROI reporting, retention/expansion), built and shipped **`11_system_health_monitor`** (`U6t0b7M6lN8eA1JO`, live and tested — executions 75/76). Original plan was to query n8n's own execution history via the `n8n` node's `resource: 'execution'` operation, but `list_credentials` showed no `n8nApi`-type credential exists in the instance and there's no MCP tool to create one — rather than stopping for that as a blocker, the design was redirected around already-available credentials (Google Sheets + Twilio): the workflow now checks **live CRM data freshness** (are there overdue appointment-reminder or follow-up-sequence rows that should have been actioned by now?) once daily, and texts the *operator* — never the client — if either looks stale. This was judged a strictly better design, not a workaround: it requires zero new credentials, and it catches a larger class of failures than execution-metadata monitoring would (e.g., a workflow that "succeeds" on each run but is quietly misconfigured to skip real rows wouldn't show up in execution history, but would show up as stale CRM data). To avoid "crying wolf," the two freshness checks **exactly mirror** the live date-parsing/threshold logic of the workflows they watch (09's 24h/2h reminder windows, 05's Day-1/3/7 follow-up thresholds — extracted via `get_workflow_details`), each with a conservative buffer (2h/1h and 30h respectively) so an alert only fires once the live workflow's *own* definition of "overdue" has been violated with room to spare. Tested twice live: once with a deliberately-stale lead row (confirmed the alert fires with a correctly-formatted, multi-issue-ready message — execution 75) and once with clean data (confirmed the chain stops naturally at zero items with **no** SMS sent — execution 76, the SDK's documented zero-item-safety pattern, no `IF`-gate needed). Documented across `README.md`, `PROJECT_STATUS.md`, `CLIENT_DEPLOYMENT_GUIDE.md` (§3 config catalog, §3d schedule table, §4 deployment order, §5 verification checklist, §6 optional-enhancements list now marks this "done"), and `ONBOARDING_SOP.md` (Phase 6 step 1 + Open Items table, now closed). **One workflow-shaped Open Item remains: the client-facing ROI report** — next in line per the user's stated priority order.
>
> **2026-06-07 — Closing the consent-language Open Item (fifth pass, same day):** Asked to keep prioritizing client support readiness, system-health monitoring, TCPA/compliance assets, ROI reporting, and retention/expansion — in that rough order of business impact, not workflow count. The fastest, lowest-risk, highest-clarity item on that list was the other purely-documentation Open Item still sitting in `ONBOARDING_SOP.md`: the standard TCPA consent-language snippet (previously "improvised per-client during Phase 3, step 4"). **`docs/SMS_CONSENT_LANGUAGE_GUIDE.md`** was written: ready-to-hand language for the actual channels a roofing company uses (digital forms, paper intake sheets, phone scripts, and the system's own first automated touch), explained clause-by-clause so the operator can defend each line if asked — plus a section reframing the system's existing opt-out backstop (the 2026-06-07 compliance fix) as a *sales* talking point ("here's exactly how we guarantee we never reply to someone who said stop, and here's the proof execution") rather than just a defensive checkbox. Like the service-agreement template, it's framed honestly as a recommendation-starting-point, not legal advice — with the operator instructed to say so out loud every time. Cross-linked into `ONBOARDING_SOP.md` (Phase 3 + Open Items, now closed), `CLIENT_ONBOARDING_INTAKE.md` (G1), `CLIENT_DEPLOYMENT_GUIDE.md` (§7, with a new line distinguishing "what the system guarantees after the fact" from "what must be collected upstream, which the system cannot retroactively create"), and `README.md`. **Both purely-documentation Open Items are now closed** — the two remaining ("system-health monitoring," "client-facing ROI report") are workflows, and are next in line specifically because the user named them as directly serving client-#1 retention, not as a default "build more workflows" move.
>
> **2026-06-07 — Closing the Phase-0 hard gate (fourth pass, same day):** Re-affirmed to keep prioritizing first-client/launch/sales readiness over workflow count, and to keep coordinating (not duplicating) with the parallel website session. Re-scanning `ONBOARDING_SOP.md`'s own "Open Items" table for the single highest-leverage closeable gap surfaced the most literal one: Phase 0 names a **hard gate** — "do not proceed to Phase 1 without a signed agreement" — and the agreement template it pointed to didn't exist yet (the workaround was "document terms via email"). **`docs/CLIENT_SERVICE_AGREEMENT_TEMPLATE.md`** was written: a structured, fillable contract draft covering scope/tier, fees & non-refundable-setup rationale, term & termination (month-to-month, 30-day notice — matching `PRICING_PACKAGING.md`), client responsibilities (including the SMS-consent split established in the compliance fix), data ownership & portability, IP, a placeholder-flagged limitation-of-liability section, and support terms — with a prominent header stating plainly that **this is a starting draft for attorney review, not a finished legal instrument**, and instructions for using it to fast-track that review rather than starting from a blank page. `ONBOARDING_SOP.md` Phase 0/Open-Items, `PRICING_PACKAGING.md`'s Contract Structure section, and `README.md`'s companion list were all updated to point to it — and the Open Items table now tracks the *real* remaining sub-task honestly: the attorney review itself, which is the one piece of this gap that genuinely cannot be completed inside an AI session. (Also corrected a stale "Tier 2"/"Tier 3" naming reference in `ONBOARDING_SOP.md` left over from before the pricing-doc reconciliation — now reads "Growth" / "Built for you" throughout, matching the live site.)
>
> **2026-06-07 — Sellability review & redirect:** Asked to evaluate the system "as if being sold to a roofing company today" rather than default to building Phase 5 next, the conclusion was that the highest-ROI next step was **not** another workflow — the automation stack (01–10) is already comprehensive and live-tested. The actual gap to a sellable, deployable V1 was (a) the complete absence of a repeatable deployment process (every workflow has Valfin-specific values hardcoded, with no catalog of what to change for client #2), and (b) one small but real SMS-compliance bug. Both were addressed immediately: **`docs/CLIENT_DEPLOYMENT_GUIDE.md`** was written (the master per-client configuration catalog + deployment order of operations + post-deploy verification plan — see §6 of that doc for the full "optional enhancements" list, which now supersedes the informal "future work" notes previously scattered through this roadmap), and the opt-out compliance fix below was shipped and verified live. **Phase 5 (Retention) remains the next workflow-building milestone, but is no longer the top priority** — packaging/onboarding/deployment work is, until a second client deployment validates the guide.
>
> **2026-06-07 — Website-coordination pass (third sellability review, same day):** Asked again to focus on website readiness, sales readiness, client-acquisition assets, deployment repeatability, and onboarding — this time explicitly told that a parallel session is actively building `website/` (a Next.js site already containing a scaffold, a live Lead Leak Calculator, all core pages, and SEO wiring — 4 commits in). Inspecting it surfaced the single highest-leverage finding of the day: **the site's primary proof asset — its flagship roofing case study (`website/src/content/results.ts` / `homepage.ts`) — is fully written but 100% gated on real measured numbers** (every stat is a literal `[X]` placeholder; the page states its own standard: "where we don't yet have verified numbers... we say so plainly"). That case study can only be filled in with data this operational track is uniquely positioned to produce. **`docs/CASE_STUDY_DATA_PLAN.md`** was written as the literal bridge — it identifies the exact six numbers needed, maps each to a concrete extraction method from the live n8n system (Communication Log timestamps, Appointments tab counts, etc.), defines a two-window measurement protocol (a pre-launch baseline window — the single most perishable, unrecoverable data point — plus a 60-90-day post-launch measurement period), and hands the resulting package to the website track without ever touching its files directly. Small, surgical additions were made to `CLIENT_ONBOARDING_INTAKE.md` (two new questions: average job value, and the client's own estimate of their pre-launch missed-call rate/booking volume — D4/D5) and `ONBOARDING_SOP.md` (Phase 1 now records the close-date/go-live-date pair and flags the baseline question as unrecoverable-if-skipped; Phase 6 now names "close the measurement period and hand off the case-study data package" as an explicit deliverable). Separately, **`docs/PRICING_PACKAGING.md` was reconciled with the live site's actual, considered pricing decisions** — it uses "Foundation/Growth/Built for you" tier names (not the working names originally drafted) and deliberately publishes no price, instead leading every conversation with the live Lead Leak Calculator. The pricing doc was rewritten to defer to those decisions explicitly: it's now framed as the seller's *private* anchor-number reference (a floor/ceiling sanity-check for "custom" quotes), never a quote sheet to publish or hand across the table.
>
> **2026-06-07 — Onboarding-the-first-client review (follow-on):** A second pass evaluated the system specifically from "what does it take to onboard our *first paying client*" — and again concluded the gap was commercial/operational, not technical. The deployment guide answers "how do we clone this," but nothing answered "what do we charge," "how do we collect a client's configuration values without an ad hoc back-and-forth," or "what's the repeatable day-by-day sequence from signed deal to live system to ongoing support." Three companion assets were written to close that gap: **`docs/PRICING_PACKAGING.md`** (tiers, suggested pricing anchored to the ROI math, contract structure), **`docs/CLIENT_ONBOARDING_INTAKE.md`** (the client-facing questionnaire that maps 1:1 onto the deployment guide's configuration catalog — turns a slow ad hoc data-collection process into a single clean packet sent on Day 1), and **`docs/ONBOARDING_SOP.md`** (the operator runbook binding all of the above plus the deployment guide into one phase-by-phase sequence from "deal closes" through "ongoing client success," and explicitly logging the "Open Items" — service agreement template, system-health monitoring, client-facing ROI report, standard TCPA consent language — that should be built before client #2 to remove the last manual workarounds). **Pricing is now the literal precondition for closing client #1** — nothing else in the funnel matters if there's no number to say out loud.



## Phase Overview

| Phase | Name | Priority | Status | Depends On |
|---|---|---|---|---|
| 1 | Google Sheets CRM | — | ✅ Done (pre-existing) | — |
| 2 | Missed-Call + Form Capture | 🔴 DEMO | ✅ **Complete — verified live** | Phase 1 |
| 3 | Lead Response + Follow-Up Automation | High | ✅ **Complete (5/5) — verified live** | Phase 2 |
| 4 | Reminders / Reschedule / Cancel | High | ✅ **Complete (2/2) — verified live** — Appointment Reminders (09) + Reschedule/Cancel (10) | Phase 3 |
| 5 | Retention (reviews, referrals, seasonal) | Low | Not started | Phase 4 |

> Note: the original Phase 4/5/6 numbering in this roadmap ("Appointment Booking + Pipeline" as Phase 4) was superseded once Appointment Booking and Pipeline Status Automation were folded into Phase 3 (workflows 06–08) — they delivered more value sooner there, alongside the rest of the lead-response stack. Phase numbering below has been compressed accordingly: former Phase 5 (Reminders/Reschedule/Cancel) is now Phase 4, and former Phase 6 (Retention) is now Phase 5.

---

## Phase 2 — Missed-Call + Form Capture ✅ COMPLETE

Verified against live n8n on 2026-06-06.

### Delivered

| Workflow | n8n ID | What It Does |
|---|---|---|
| CRM Adapter | `wVRHChyFrUNRaH4M` | Google Sheets sub-workflow. Upserts leads, mints LEAD-####, logs comm entries. `skipLeadCreation` routing ensures missed calls never create Lead records. |
| Form Capture + Confirmation (formerly "Form Capture + AI Scoring") | `HdJc5cy8cmqMBfGR` | Dual entry (n8n form + webhook) → CRM upsert → Haiku 4.5 confirmation SMS → outbound log → Every Lead Alert. **V1.1:** AI scoring removed; every lead follows this same path. |
| Missed-Call Auto-SMS | `u9I1bqrLW6V5LtLp` | Twilio call-status → no-answer/busy filter → static SMS → Comm Log only (no Lead). |

### Completion Criteria (All Met)
- [x] Form submitted → lead in Sheets → AI score → confirmation SMS sent
- [x] Missed call → auto SMS within seconds → Comm Log entry written (no Lead created)
- [x] All workflows active in n8n
- [x] Google Sheets, Anthropic, and Twilio credentials set
- [x] Twilio call-status webhook URL configured
- [x] `skipLeadCreation` routing live and tested

### Open Item (Non-Blocking)
- [ ] Twilio toll-free number verification — error 30032 blocks SMS delivery at carrier level. Complete at twilio.com/console. Workflows are ready and correct. **User confirmed (2026-06-07): explicitly non-blocking — external infrastructure item, do not block development on it.**

---

## Phase 3 — Lead Response + Follow-Up Automation ✅ COMPLETE (5/5)

**Goals:** Reduce lead response time, ensure no qualified lead is forgotten, increase appointment booking rate, give the owner pipeline visibility without opening Sheets.

Verified against live n8n on 2026-06-07. All five components published, active, and (where execution-testable) confirmed against live data.

### Delivered

| Workflow | n8n ID | What It Does |
|---|---|---|
| Every Lead Alert (formerly "Hot Lead Alert") | `KIpMMKM8H5IZB9wb` | Sub-workflow called by 02 for every submission. **V1.1:** Hot/Emergency branching removed — emails the owner a branded summary by default (SMS built but disabled by default). Owner contact `+18575261499` set via `CONFIG`. |
| Follow-Up Sequence | `chYfABnQdnPfiHQx` | Daily 9 AM ET. Reads all New/Contacted leads, filters by time thresholds (Day 1/3/7), sends personalized static SMS templates, updates lead status + Follow-up Count via CRM Adapter. Stops at 3 attempts or status change. Booked leads auto-excluded. |
| Appointment Booking | `ax2sMbvv0lqyJHMg` | Owner-facing n8n form. Looks up lead by ID, writes row to Appointments tab, sends customer confirmation SMS, updates lead status to Booked via CRM Adapter. **V1.1:** structured calendar-picker date + 30-min `dropdown` time slots (8 AM–5 PM, `America/New_York`), configurable via `CONFIG`. **Tested end-to-end in production — confirmed working** (Lead → Booked, Appointment row, Comm Log entry, Follow-Up + Every Lead Alert unaffected). |
| Pipeline Status Digest | `ehqNYjZRirX5L3sX` | Daily 6 PM ET. Reads all leads, tallies counts by status (New/Contacted/Booked/Stale), lists Stale leads by name + phone, reports today's new leads and bookings. **V1.1:** emails the owner a branded digest by default (SMS built but disabled by default). Read-only; no Sheets writes. Owner contact `+18575261499` set via `CONFIG`. |
| Weekly Pipeline Report | `Y7ruzhYGMhE001fr` | Weekly Monday 8 AM ET. Reads all leads, computes trailing-7-day metrics (new leads, bookings, stale count, bookings/new ratio, top lead sources). **V1.1:** emails the owner a branded weekly report by default (SMS built but disabled by default; Hot/Emergency split metric removed). Owner contact synced programmatically via `CONFIG` (zero manual setup). **Test-executed live (execution 54): correct report computed and sent successfully.** |

### Phase 3 Completion Criteria — All Met ✅
- [x] Every lead triggers an immediate owner notification (**V1.1:** email by default, SMS built but disabled by default — was SMS-only for Hot leads)
- [x] Automated Day 1 / Day 3 / Day 7 follow-up sequence running
- [x] Appointment Booking Workflow live — form, Appointments tab write, customer SMS, lead status update
- [x] Appointment Booking verified end-to-end in production (Lead → Booked, Appointment row, Comm Log, Follow-Up + Hot Alert unaffected)
- [x] Every interaction logged to Communication Log
- [x] CRM Adapter supports `followUpCount` from callers
- [x] Booked leads automatically excluded from follow-up sequence
- [x] Daily pipeline digest live — counts by status + Stale/Hot escalation + today's activity
- [x] Weekly pipeline report live — trailing 7-day metrics, tested against live data
- [x] Owner phone (`+18575261499`) configured and confirmed working across workflows 04, 07, 08

**Phase 3 has zero open setup items.** All `OWNER_PHONE_HERE` placeholders are resolved.

---

## Phase 4 — Reminders / Reschedule / Cancel ✅ COMPLETE (2/2)

### Scope
- 24-hour appointment reminder SMS
- 2-hour reminder SMS
- Reschedule flow: keyword reply ("reschedule", "can't make it") → propose new slot
- Cancel flow: confirmation + optional rebooking offer

### Key Workflows
| Workflow | n8n ID | Status | What It Does |
|---|---|---|---|
| `09_appointment_reminders` | `bJcO5ox2u190bxTr` | ✅ **Live — tested live (execution 55)** | Hourly schedule → reads Appointments tab → computes 24h (20–28h out) and 2h (1–3h out) reminder windows from parsed `Appt Date`/`Appt Time` → sends personalized SMS per appointment → flags `Reminder 24h`/`Reminder 2h` columns to prevent duplicate sends. |
| `10_reschedule_cancel` | `Bj5b3sUexa8EeQcK` | ✅ **Live — tested live (executions 63/64/65; compliance fix verified 67/68)** | Inbound SMS (Twilio Trigger, `com.twilio.messaging.inbound-message.received`) → keyword-classifies reschedule vs. cancel vs. other → finds the customer's nearest upcoming Scheduled appointment by phone → updates Status (`Cancelled` on cancel; unchanged on reschedule, left to staff) + appends a timestamped Notes entry → replies to the customer (cancel: confirms + invites rebooking; reschedule: acknowledges + promises a callback) → alerts the owner by SMS so staff can act immediately. Not-found and irrelevant-text cases handled gracefully (generic reply / silent ignore). |

### Why Acknowledge-and-Alert (not full self-service rebooking)
Building a true two-way SMS slot-negotiation flow (propose times, parse the customer's reply, re-check availability, confirm) is high-complexity, high-risk-of-confusion over SMS, and low marginal value versus a simpler high-confidence design: instantly acknowledge the customer (so they feel heard and stop worrying about a no-show penalty) **and** instantly alert the owner with full context (name, phone, appointment, request type) so staff can close the loop with a phone call — the channel roofing customers actually expect for scheduling changes. This mirrors the static-template-no-AI precedent (03/05/06/07/08/09): reliable, zero-cost, fast, and impossible to derail with an unexpected reply.

### Prerequisite Fix (Completed 2026-06-07)
Workflow 06's Booking Form originally captured `Appointment Date`/`Appointment Time` as **free-text** fields (placeholders "e.g. Tuesday, June 10" / "e.g. 2:00 PM"), producing strings that could not be reliably parsed for computing "24 hours before" / "2 hours before" reminder timing. Fixed at the source: the form now uses a `date` field type (`YYYY-MM-DD`, machine-parseable) and a `dropdown` field type (fixed hourly time slots, 8 AM–5 PM). `Build Booking Payload` derives a friendly display string (`formatFriendlyDate()` → "Tuesday, June 10") for the customer-facing confirmation SMS, so the user experience is unchanged while the underlying stored data is now parseable. Republished and live-tested.

### Compliance Fix (Completed 2026-06-07 — surfaced by sellability review)
A pre-launch evaluation of the system "as if being sold to a roofing company today" surfaced a real TCPA/carrier-compliance exposure: `Normalize Inbound SMS`'s original cancel-intent regex included the bare word `stop`, so a standalone opt-out text like `"STOP"` would have been misclassified as "cancel my appointment" and triggered an automated reply — the opposite of compliant behavior (an automated system must never auto-reply to, or misclassify, a carrier opt-out signal). Patched live via `update_workflow setNodeParameter` (republished `activeVersionId: 91bb00e6-...`): the node now detects standalone opt-out keywords (`STOP`, `STOPALL`, `UNSUBSCRIBE`, `QUIT`, `END`, `CANCEL ALL`, `OPT-OUT`, `REMOVE` — matched only when they constitute the *entire* trimmed message via an anchored regex) and routes them to `intent: 'other'` (silently ignored, zero auto-reply) — a defense-in-depth backstop alongside Twilio's carrier-level Advanced Opt-Out handling. Verified live: execution 67 (`"STOP"` → `intent: 'other'`, no reply) and execution 68 (`"please cancel my roofing appointment, something came up"` → `intent: 'cancel'`, full flow ran correctly — proving "cancel" inside a real sentence is still handled as an appointment request, not an opt-out). Documented in `docs/CLIENT_DEPLOYMENT_GUIDE.md` §7 as a per-client go-live checklist item.

### Dependencies — All Resolved ✅
- ✅ Phase 3 appointment booking complete (workflow 06, `ax2sMbvv0lqyJHMg`, tested end-to-end)
- ✅ Workflow 06 form fields upgraded to structured date/dropdown — prerequisite for workflow 09, delivered 2026-06-07
- ✅ Inbound SMS trigger — used native `n8n-nodes-base.twilioTrigger` (`com.twilio.messaging.inbound-message.received`) with the existing `twilioApi` credential. **Zero manual setup required** — no Twilio console webhook URL configuration needed, unlike a generic webhook approach. Confirmed compatible with current trial-account status (inbound SMS does not require toll-free verification — only outbound to unverified numbers is restricted).

**Phase 4 has zero open setup items.**

---

## Phase 5 — Retention

### Scope
- Post-job review request SMS at Day 3 and 14 after job completion
- Referral invite SMS
- Seasonal outreach (pre-storm, spring inspection offer)
- Re-engagement for cold/dormant leads

### Key Workflows
- `11_post_job_retention`
- `12_seasonal_campaigns`

### Dependencies
- Phase 4 complete
- Job completion tracking in `Jobs` tab

---

## Recommended Build Order

```
Phase 2 ✅ → Phase 3 ✅ COMPLETE (Hot Alert + Follow-Up + Booking + Pipeline Digest + Weekly Report, all live & tested)
   → Phase 4 ✅ COMPLETE (Appointment Reminders 09 ✅ + Reschedule/Cancel 10 ✅, both live & tested)
   → [CURRENT PRIORITY] Commercial/onboarding readiness — NOT a workflow phase:
        Pricing & Packaging ✅ → Client Onboarding Intake ✅ → Onboarding SOP ✅
        → close client #1 → validate the whole sequence end-to-end → THEN:
   → Phase 5 (Retention) — resumes once client #1 is live and the onboarding
     sequence above has been proven on a real engagement (not before — building
     more workflows before you can sell/onboard the existing ten is optimizing
     the wrong constraint)
```

**Why this re-ordering:** Two consecutive sellability reviews (2026-06-07) reached the same conclusion from different angles — the bottleneck to revenue is not "more automation," it's "the ability to price, sign, onboard, and support a client using what's already built." `docs/PRICING_PACKAGING.md`, `docs/CLIENT_ONBOARDING_INTAKE.md`, and `docs/ONBOARDING_SOP.md` close that gap. Phase 5 is real, valuable future work — but building it before client #1 exists would be building inventory nobody has asked for yet, while the actual precondition for revenue (a price, a contract, a repeatable onboarding motion) sat unbuilt.

**Rule of thumb:** Each phase must be live and tested before building the next.
The CRM Adapter (`wVRHChyFrUNRaH4M`) is the foundation — all workflows call it. Never write directly to Google Sheets from a non-adapter workflow.

**Owner-notification pattern (established across 04/07/08):** When adding a new SMS-sending workflow, prefer reading the live `OWNER_PHONE` value from an already-configured workflow (e.g. `KIpMMKM8H5IZB9wb` → `Build Alert Message`) and patching it in via `update_workflow` `setNodeParameter` — this avoids a repeat manual setup step for the user. Only fall back to a placeholder + manual-action checklist if no prior workflow has the value yet.

---

## Architectural Decisions (Locked)

| Decision | Choice | Rationale |
|---|---|---|
| CRM backend | Google Sheets (now) → GoHighLevel (future) | Swap isolated to CRM Adapter only |
| AI scoring | Claude Sonnet 4.6 | Judgment calls warrant mid-tier model |
| AI SMS copy — form confirmation | Claude Haiku 4.5 | Volume/speed, lower cost |
| AI SMS copy — missed call | None — static message | User decision after testing: simpler, faster, zero cost |
| AI SMS copy — follow-up sequences | None — static templates | Reliability, zero cost, consistent batch behavior |
| JSON enforcement | `output_config.format` (API-level JSON schema) | Guaranteed parseable JSON, not prompt-dependent |
| Form hosting | n8n Form Trigger + parallel POST webhook | Demo now, website embed later at zero cost |
| Missed-call detection | n8n side only (Twilio call-status webhook) | Clean separation; Twilio only notifies, n8n decides |
| Workflow structure | Sub-workflow pattern for all CRM I/O | Modularity for future CRM swap |
| Lead creation rule | Form submission only | Missed calls → Comm Log only. No phantom leads from unanswered calls. |
| `skipLeadCreation` detection | `source === 'Phone' && logSummary === 'Missed call — auto-SMS sent'` | Keep this string identical in workflow 01 and 03 or routing breaks |
| Owner notification channel | SMS via Twilio (Phase 3) | Simplest, uses existing credentials. Designed for future Slack/email expansion. |
| Hot lead threshold | `temperature === 'Hot'` OR `urgency === 'Emergency'` | Captures both scored and keyword-emergency leads |
| Follow-up stop conditions | Count ≥ 3 OR status not in {New, Contacted} | Prevents over-messaging; status change by any path stops the sequence |
| Pipeline digest schedule | Daily 6 PM ET (22:00 UTC) | End-of-business-day summary — captures the full day's activity before owner's evening review |
| Pipeline digest delivery | SMS, plain text, no AI, read-only | Reliability + zero cost; matches static-template precedent from workflows 03/05/06; GSM-7 encoding minimizes segment count |
| Stale escalation scope | Stale AND Temperature in {Hot, Warm} only | Surfaces revenue-at-risk leads without noise from expected Cold churn |
| Weekly report delivery channel | SMS (not email) | No email credential exists in n8n; SMS reuses proven Twilio infrastructure with zero new setup. Roadmap explicitly allowed either channel. |
| Weekly report schedule | Monday 8 AM ET (13:00 UTC) | Start-of-week look-back at the prior 7 days — gives the owner a trend view to open the week with |
| New-workflow owner-phone setup | Read live value from an existing configured workflow + patch via `update_workflow`, when possible | Eliminates repeat manual setup steps (used for workflow 08 — zero manual action required, unlike 04/07) |
| Booking form date/time capture | Structured `date` + `dropdown` fields (not free text) | Free-text values like "Tuesday, June 10" / "2:00 PM" placeholders are not reliably parseable for reminder-time math; structured fields guarantee `YYYY-MM-DD` / fixed time-slot strings while a derived friendly-display string keeps the customer SMS unchanged |
| Reminder schedule cadence | Hourly check (not per-appointment scheduling) | Simpler, stateless, self-healing — a single recurring poll covers both the 24h and 2h windows for every appointment without per-appointment trigger management; 4–8 hour windows (20–28h, 1–3h) tolerate the hourly granularity |
| Reminder idempotency | `Reminder 24h`/`Reminder 2h` sheet flags written with ISO timestamps, checked before every send | Prevents duplicate SMS on repeat hourly runs; matches the reserved-column design already present in the Appointments tab from workflow 06 |
| Reminder send failure isolation | Each reminder processed independently in a `splitInBatches(1)` loop with `retryOnFail` | One customer's bad phone number or a transient Twilio error doesn't block reminders to other customers in the same run |
| Inbound SMS trigger | Native `n8n-nodes-base.twilioTrigger` (`com.twilio.messaging.inbound-message.received`) over a generic webhook | Reuses the existing `twilioApi` credential and requires zero manual Twilio-console webhook configuration — eliminates a manual setup step entirely (vs. a generic webhook node, which would need the user to paste an n8n URL into the Twilio console) |
| Reschedule/Cancel reply strategy | Acknowledge customer + alert owner (no AI, no self-service slot negotiation) | Two-way SMS slot negotiation is high-complexity and error-prone over SMS; instant acknowledgment + an immediate, fully-contextualized owner alert delivers the real business value (customer feels heard, staff closes the loop by phone — the channel customers expect for schedule changes) at zero cost and zero risk of a derailed conversation |
| Reschedule/Cancel intent classification | Keyword regex matching (no AI) | Matches the static-template-no-AI precedent (03/05/06/07/08/09); "cancel"/"reschedule" intents are linguistically distinct enough that keyword matching is reliable, instant, and free — reserving AI spend for judgment calls (lead scoring) where it earns its cost |
| Reschedule/Cancel appointment matching | Phone-number lookup against `Status = 'Scheduled'` rows, nearest-upcoming-first | Customers text from the same number they booked with; matching + sorting by date/time picks the most relevant appointment when a customer has multiple, and ignores already-cancelled/completed rows |
| Reschedule/Cancel sheet update | Single `googleSheets update` writing `Status` + `Notes` (timestamped, appended) per request | Cancel flips `Status` to `Cancelled` (removes the slot from reminders/digests automatically); reschedule preserves `Status = 'Scheduled'` (staff coordinates the new time) while both leave a permanent audit trail in `Notes` |

### V1.1 (2026-06-11) — Decisions Superseded Above

| Superseded decision (row above) | New decision | Rationale |
|---|---|---|
| "AI scoring — Claude Sonnet 4.6" | **Removed entirely.** No AI lead scoring anywhere. | Simplification — founder feedback was that scoring added complexity and judgment-call maintenance without proportional value; every lead gets the same fast notification instead |
| "Hot lead threshold — `temperature === 'Hot'` OR `urgency === 'Emergency'`" | **Removed.** "Every Lead Alert" (04) fires unconditionally for every submission. | No scores/temperatures exist to threshold against |
| "Owner notification channel — SMS via Twilio (Phase 3)" / "Pipeline digest delivery — SMS, plain text" / "Weekly report delivery channel — SMS" | **Email is the default channel for all owner/client notifications and reports** (04, 07, 08, 11, 12); SMS is built but disabled by default, toggleable per client via a centralized `CONFIG` block | Email is free, supports richer branded formatting (subject lines, tables, future charts), and doesn't depend on Twilio toll-free verification; SMS remains available for clients who want it |
| "Stale escalation scope — Stale AND Temperature in {Hot, Warm} only" | Pipeline Status Digest (07) lists **all** Stale leads by name + phone, no temperature filter | `Temperature` no longer exists |
| "Reschedule/Cancel intent classification ... reserving AI spend for judgment calls (lead scoring)" | Rationale text is historical — no AI spend exists anywhere in the system post-V1.1 (only Haiku 4.5 for the form-confirmation SMS) | AI scoring removed |
